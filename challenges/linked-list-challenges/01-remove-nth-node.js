/**
 * CHALLENGE 1: REMOVE NTH NODE FROM END
 * ======================================
 * Remove the n-th node from the end of a singly linked list and return the head.
 *
 * Problem:
 * Given the head of a linked list, remove the n-th node from the end and
 * return its head.
 *
 * Example:
 * Input: 1 -> 2 -> 3 -> 4 -> 5, n = 2
 * Output: 1 -> 2 -> 3 -> 5
 *
 * Constraints:
 * - The number of nodes in the list is in the range [1, 10^5]
 * - 1 <= n <= list length
 *
 * Big O Hints:
 * - Time Complexity: O(n) - use one-pass two-pointer (fast/slow) approach
 * - Space Complexity: O(1) - constant extra space
 *
 * DO NOT: rely on converting to arrays for the main solution
 */

// ============================================================================
// YOUR SOLUTION HERE
// ============================================================================

/**
 * @param {object|null} head  // List node with `.value` and `.next`
 * @param {number} n
 * @return {object|null}
 */
export function removeNthFromEnd(head, n) {
  // TODO: Implementation required
  throw new Error('Not implemented');
}
