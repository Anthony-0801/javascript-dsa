/**
 * CHALLENGE 6: DETECT CYCLE IN LINKED LIST
 * ======================================
 * Use Floyd's Tortoise and Hare to detect cycles and find the cycle start.
 *
 * Problem:
 * Implement `hasCycle(head)` that returns true if the list contains a cycle,
 * and `detectCycleStart(head)` that returns the node where the cycle begins
 * (or `null` if no cycle).
 *
 * Big O Hints:
 * - Time: O(n)
 * - Space: O(1)
 */

// ============================================================================
// YOUR SOLUTION HERE
// ============================================================================

/**
 * @param {object|null} head
 * @return {boolean}
 */
export function hasCycle(head) {
  // TODO: Implementation required
  throw new Error('Not implemented');
}

/**
 * @param {object|null} head
 * @return {object|null}
 */
export function detectCycleStart(head) {
  // TODO: Implementation required
  throw new Error('Not implemented');
}
