/**
 * CHALLENGE 3: DELETE NODE WITHOUT HEAD
 * =====================================
 * Given only access to a node (not the head), delete that node from the
 * singly linked list. You cannot access the head of the list.
 *
 * Problem:
 * Given a node in the middle of a singly linked list (guaranteed not to be
 * the tail), delete it in-place.
 *
 * Example:
 * Input: node with value 5 in list 4 -> 5 -> 1 -> 9
 * After: 4 -> 1 -> 9
 *
 * Big O Hints:
 * - Time Complexity: O(1)
 * - Space Complexity: O(1)
 */

// ============================================================================
// YOUR SOLUTION HERE
// ============================================================================

/**
 * @param {object} node  // node to delete (not tail)
 * @return {boolean} true if deletion succeeded
 */
export function deleteNodeWithoutHead(node) {
  // TODO: Implementation required
  throw new Error('Not implemented');
}
