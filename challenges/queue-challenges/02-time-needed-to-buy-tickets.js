/**
 * CHALLENGE 2: TIME NEEDED TO BUY TICKETS
 * ======================================
 * Simulate a queue at a ticket counter: each person needs a certain number
 * of tickets, and each second the person at front buys 1 ticket. After buying
 * one, if they still need more, they go to the back of the queue.
 *
 * Problem:
 * - `tickets` is an array where tickets[i] is number of tickets person i needs.
 * - `k` is the index of the person we want to measure.
 * Return the total time (seconds) until person `k` finishes buying all tickets.
 *
 * Example:
 * tickets = [2,3,2], k = 2 => output: 6
 */

// ============================================================================
// YOUR SOLUTION HERE
// ============================================================================

/**
 * @param {number[]} tickets
 * @param {number} k
 * @return {number}
 */
export function timeNeededToBuyTickets(tickets, k) {
  throw new Error('Not implemented');
}
