/**
 * CHALLENGE 4: REVERSE FIRST K ELEMENTS OF QUEUE
 * =============================================
 * Given a queue represented as an array, reverse the order of the first k
 * elements, leaving the rest in the same order. Use only queue operations
 * (enqueue/dequeue) and a stack if needed.
 *
 * Example:
 * Input: queue = [1,2,3,4,5], k = 3 => Output: [3,2,1,4,5]
 */

// ============================================================================
// YOUR SOLUTION HERE
// ============================================================================

/**
 * @param {number[]} queueArr
 * @param {number} k
 * @return {number[]} modified queue as array
 */
export function reverseFirstK(queueArr, k) {
  throw new Error('Not implemented');
}
