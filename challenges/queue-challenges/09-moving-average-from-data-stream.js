/**
 * CHALLENGE 9: MOVING AVERAGE FROM DATA STREAM
 * ===========================================
 * Given a stream of numbers and a window size `k`, compute the moving average
 * for each newly seen element. Return an array of moving averages for the
 * sequence.
 *
 * Example: nums = [1,3,2,6], k = 3 => [1,2,2, (3+2+6)/3 = 11/3]
 */

// ============================================================================
// YOUR SOLUTION HERE
// ============================================================================

/**
 * @param {number[]} nums
 * @param {number} k
 * @return {number[]}
 */
export function movingAverage(nums, k) {
  throw new Error('Not implemented');
}
