/**
 * CHALLENGE 5: HOT POTATO
 * =======================
 * Simulate the children's game "Hot Potato": children stand in a circle and
 * pass the potato; after `num` passes the child holding the potato is removed.
 * Continue until one child remains; return the name of the winner.
 *
 * Example:
 * names = ['A','B','C','D'], num = 3 => winner: 'A'
 */

// ============================================================================
// YOUR SOLUTION HERE
// ============================================================================

/**
 * @param {string[]} names
 * @param {number} num
 * @return {string}
 */
export function hotPotato(names, num) {
  throw new Error('Not implemented');
}
