/**
 * CHALLENGE 7: LONGEST SUBARRAY WITH SUM K
 * =======================================
 * Find the length of the longest contiguous subarray that sums to K.
 *
 * Problem:
 * Given an integer array and an integer K, return the length of the longest
 * contiguous subarray whose sum equals K.
 *
 * Constraints:
 * - Array length: 0..1e5
 * - Values: -1e6..1e6
 *
 * Expected Complexity:
 * - Time: O(n) using prefix-sum + hash map
 * - Space: O(n)
 *
 * DO NOT use nested loops (O(n^2))
 */

// ============================================================================
// YOUR SOLUTION HERE
// ============================================================================

/**
 * @param {number[]} arr
 * @param {number} k
 * @return {number}
 */
export function longestSubarraySumK(arr, k) {
  // TODO: Implementation required
  throw new Error('Not implemented');
}
