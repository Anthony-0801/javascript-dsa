/**
 * CHALLENGE 6: INTERSECTION OF ARRAYS
 * ==================================
 * Compute the intersection of two arrays (unique elements only).
 *
 * Problem:
 * Given two integer arrays `nums1` and `nums2`, return an array of their intersection.
 * Each element in the result must be unique. The result can be in any order.
 *
 * Example:
 * Input: nums1 = [1,2,2,1], nums2 = [2,2]
 * Output: [2]
 *
 * Constraints:
 * - 0 <= nums1.length, nums2.length <= 10^5
 * - -10^9 <= nums[i] <= 10^9
 *
 * Big O Hints:
 * - Time Complexity: O(n + m)
 * - Space Complexity: O(n) for a set or map
 */

// ============================================================================
// YOUR SOLUTION HERE
// ============================================================================

/**
 * @param {number[]} nums1
 * @param {number[]} nums2
 * @return {number[]}
 */
export function intersection(nums1, nums2) {
  // TODO: Implementation required
  throw new Error('Not implemented');
}

// ============================================================================
// EXAMPLE USAGE (for testing)
// ============================================================================

/*
console.log(intersection([1,2,2,1],[2,2])); // [2]
*/

export default intersection;
