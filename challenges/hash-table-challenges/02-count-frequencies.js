/**
 * CHALLENGE 2: COUNT FREQUENCIES
 * ==============================
 * Count how many times each element appears in the input array.
 *
 * Problem:
 * Given an array `arr`, return an object (map) where keys are the elements from `arr`
 * and values are their frequency counts.
 *
 * Example:
 * Input: ['a','b','a','c','b','a']
 * Output: { a: 3, b: 2, c: 1 }
 *
 * Constraints:
 * - 0 <= arr.length <= 10^5
 * - Elements may be strings or numbers (treat them as map keys)
 *
 * Big O Hints:
 * - Time Complexity: O(n)
 * - Space Complexity: O(n)
 */

// ============================================================================
// YOUR SOLUTION HERE
// ============================================================================

/**
 * @param {Array<string|number>} arr
 * @return {Object}
 */
export function countFrequencies(arr) {
  // TODO: Implementation required
  throw new Error('Not implemented');
}

// ============================================================================
// EXAMPLE USAGE (for testing)
// ============================================================================

/*
console.log(countFrequencies(['a','b','a','c','b','a'])); // { a: 3, b: 2, c: 1 }
*/

export default countFrequencies;
