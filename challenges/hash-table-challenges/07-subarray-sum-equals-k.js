/**
 * CHALLENGE 7: SUBARRAY SUM EQUALS K
 * ==================================
 * Count the number of continuous subarrays that sum to k.
 *
 * Problem:
 * Given an array of integers `nums` and an integer `k`, return the total number of continuous subarrays whose sum equals `k`.
 *
 * Example:
 * Input: nums = [1,1,1], k = 2
 * Output: 2
 *
 * Constraints:
 * - 1 <= nums.length <= 10^5
 * - -10^4 <= nums[i] <= 10^4
 * - -10^9 <= k <= 10^9
 *
 * Big O Hints:
 * - Time Complexity: O(n) using prefix-sum + hashmap
 * - Space Complexity: O(n)
 */

// ============================================================================
// YOUR SOLUTION HERE
// ============================================================================

/**
 * @param {number[]} nums
 * @param {number} k
 * @return {number}
 */
export function subarraySumEqualsK(nums, k) {
  // TODO: Implementation required
  throw new Error('Not implemented');
}

// ============================================================================
// EXAMPLE USAGE (for testing)
// ============================================================================

/*
console.log(subarraySumEqualsK([1,1,1],2)); // 2
*/

export default subarraySumEqualsK;
