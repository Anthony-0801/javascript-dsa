/**
 * CHALLENGE 9: TOP K FREQUENT ELEMENTS
 * ====================================
 * Return the k most frequent elements from an array.
 *
 * Problem:
 * Given an integer array `nums` and an integer `k`, return the `k` most frequent elements.
 * You may return the answer in any order.
 *
 * Example:
 * Input: nums = [1,1,1,2,2,3], k = 2
 * Output: [1,2]
 *
 * Constraints:
 * - 1 <= nums.length <= 10^5
 * - k is in range [1, number of unique elements]
 *
 * Big O Hints:
 * - Time Complexity: O(n log k) using a heap or O(n) using bucket sort
 * - Space Complexity: O(n)
 */

// ============================================================================
// YOUR SOLUTION HERE
// ============================================================================

/**
 * @param {number[]} nums
 * @param {number} k
 * @return {number[]}
 */
export function topKFrequent(nums, k) {
  // TODO: Implementation required
  throw new Error('Not implemented');
}

// ============================================================================
// EXAMPLE USAGE (for testing)
// ============================================================================

/*
console.log(topKFrequent([1,1,1,2,2,3], 2)); // [1,2]
*/

export default topKFrequent;
