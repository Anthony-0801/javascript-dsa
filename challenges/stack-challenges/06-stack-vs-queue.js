/**
 * CHALLENGE 6: STACK VS QUEUE
 * ===========================
 * Implement a function that, given an array of operations, simulates the
 * behavior of both a stack and a queue and returns which structure produced
 * the observed outputs. This helps illustrate differences in ordering.
 *
 * Input format (example):
 * {
 *   ops: ['push 1','push 2','pop','push 3','pop'],
 *   outputs: [2,3]
 * }
 *
 * Return 'stack' if stack semantics match outputs, 'queue' if queue semantics
 * match, 'both' if both match, or 'neither'.
 *
 * Big O Hints:
 * - Time Complexity: O(n)
 * - Space Complexity: O(n)
 */

// ============================================================================
// YOUR SOLUTION HERE
// ============================================================================

/**
 * @param {{ops:string[], outputs:number[]}} input
 * @return {'stack'|'queue'|'both'|'neither'}
 */
export function detectDataStructure(input) {
  throw new Error('Not implemented');
}
