/**
 * CHALLENGE 3: BACKSPACE STRING COMPARE
 * ====================================
 * Given two strings s and t, return true if they are equal when both are
 * typed into empty text editors. '#' means a backspace character.
 *
 * Example:
 * Input: s = "ab#c", t = "ad#c"
 * Output: true  (both become "ac")
 *
 * Constraints:
 * - 1 <= s.length, t.length <= 200
 *
 * Big O Hints:
 * - Time Complexity: O(n)
 * - Space Complexity: O(1) (using two-pointer backwards approach) or O(n)
 */

// ============================================================================
// YOUR SOLUTION HERE
// ============================================================================

/**
 * @param {string} s
 * @param {string} t
 * @return {boolean}
 */
export function backspaceCompare(s, t) {
  throw new Error('Not implemented');
}
