/**
 * CHALLENGE 8: SORT STACK
 * =======================
 * Sort a stack so that the smallest elements are on the top using only one
 * additional stack (no other data structures like arrays allowed).
 *
 * Input: stack represented as array (bottom->top)
 * Output: new sorted stack array (bottom->top) with smallest on top
 *
 * Big O Hints:
 * - Time Complexity: O(n^2) worst-case
 * - Space Complexity: O(n) additional stack
 */

// ============================================================================
// YOUR SOLUTION HERE
// ============================================================================

/**
 * @param {number[]} stackArr
 * @return {number[]}
 */
export function sortStack(stackArr) {
  throw new Error('Not implemented');
}
